

Program uno;

Var 
  num1: Integer;
  num2: integer;
Begin
  Read(num1);
  Read(num2);
  If (num1 > num2) Then
    Write(num1)

  Else If (num2 > num1)
         Then
         write(num2)
  Else
    write('los numeros son iguales')
End.
